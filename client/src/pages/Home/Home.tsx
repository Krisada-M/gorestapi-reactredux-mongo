import React from 'react';

export interface HomeProps{}

const Home: React.FunctionComponent<HomeProps> = (props) => {
    return (
        <div>
            Home
        </div>
    );
};

export default Home;